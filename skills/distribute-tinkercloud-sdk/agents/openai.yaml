interface:
  display_name: "Distribute Tinkercloud SDK"
  short_description: "Prepare and release the TypeScript client SDK"
  default_prompt: "Use $distribute-tinkercloud-sdk to prepare and verify the TypeScript client package without publishing it."
