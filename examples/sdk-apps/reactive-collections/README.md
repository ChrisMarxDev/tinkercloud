# Reactive collections

A compact shared checklist that proves collection create, optimistic update,
delete, and snapshot recovery after a live reconnect. Its manifest enables
Tinkercloud's built-in `kv` data primitive and `realtime` freshness hints; it does
not select a database, app identity, or socket credential.
